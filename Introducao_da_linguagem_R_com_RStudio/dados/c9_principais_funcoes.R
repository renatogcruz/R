esoph
# default 6 linhas
head(esoph)
tail(esoph)

# 10 linhas
head(esoph, n=10)

#mostra a dimensão de um conjunto (linhas x colunas)
dim(esoph)

#comprimento de um vetor
length(islands)

#nomes das colunas e linhas de um conj
colnames(esoph)
rownames(esoph)

#resumo estatistico de um conjunto de dados
summary(esoph)

x = file.choose()
x


